# location on the server's file system where the disk images exist (no trailing
# slash)
IMGDIR_LOCAL = '/home/bob/projects/retrospective/little-image-manager/tests'

#location on the webserver that corresponds to the IMGDIR_LOCAL
IMGDIR_URI = 'http://localhost/~bob/lim/'
